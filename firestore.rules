rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /museumIcons/{document=**} {
      // 誰でも読み取り可能
      allow read: if true;
      // 書き込みは認証されたユーザーのみ可能
      allow write: if request.auth != null;
    }
  }
}
